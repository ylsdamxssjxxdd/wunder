# 中级晋升任务执行说明（V2026-07）

任务描述：  
`将 2026 年 7 月份可以晋升中级的人员名单放到 Excel 中。`

## 1. 必须遵守的执行参数

- cutoff_date：`2026-07-31`
- target_level：`中级`
- rule_version：`V2026-07`
- 数据库：`hr_promotion_sim`

## 2. 推荐执行顺序

1. 查询规则文档，确认 RuleID 与阈值；
2. 从基础表计算候选与评分；
3. 生成最终名单；
4. 导出 Excel（字段顺序固定）；
5. 与 `promotion_baseline_202607` 对拍，输出差异统计。

## 3. 输出字段顺序（固定）

1. `employee_id`
2. `name`
3. `dept_id`
4. `current_level`
5. `target_level`
6. `eligible`
7. `total_score`
8. `hit_rule_ids`
9. `evidence_summary`
10. `cutoff_date`

## 4. 结果解释要求

- 每条记录需可追溯到对应 RuleID；
- 不允许只给结论不提供依据；
- 若规则冲突或数据缺失，必须标注影响范围。

## 5. 验收标准

- 结果文件可正常打开；
- 名单行数与 `promotion_baseline_202607` 中 `eligible=1` 行数一致；
- 抽检样本的 `hit_rule_ids` 与数据库事实一致。

