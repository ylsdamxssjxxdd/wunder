# 中级晋升模拟政策总则（V2026-07）

文档编号：`HR-PROMO-MID-2026-07`  
生效日期：`2026-01-01`  
评审截止：`2026-07-31 23:59:59`  
适用范围：仅用于“2026 年 7 月可晋升中级人员名单”模拟任务。

## 1. 目标

建立可机读、可审计、可复现的晋升规则体系，确保智能体可基于数据库与规则文档独立完成名单计算与 Excel 输出。

## 2. 基本定义

- 当前职级：`初级/中级/高级`。
- 目标职级：本轮固定为 `中级`。
- 截止日期：`2026-07-31`。
- 合格判定：`硬性规则全部通过` 且 `综合评分 >= 78`。

## 3. 硬性规则

1. `PROM-MID-HARD-001`：员工在截止日期时为在职正式员工（`status=在职 AND employment_type=正式`）。
2. `PROM-MID-HARD-002`：当前为初级，且初级任职时长不少于 24 个月。  
   允许例外：`PROM-MID-EXC-001`。
3. `PROM-MID-HARD-003`：最近四个季度（`2025Q3~2026Q2`）绩效等级均不低于 B，且 A/S 季度数量不少于 2。
4. `PROM-MID-HARD-004`：2025 年通过培训学时不少于 40 小时。  
   允许例外：`PROM-MID-EXC-002`。
5. `PROM-MID-HARD-005`：近 12 个月内（`2025-08-01~2026-07-31`）无有效“记过/严重违纪”处分。
6. `PROM-MID-HARD-006`：截止日期时持有有效的必需认证（`is_required_for_promotion=1`）。  
   允许例外：`PROM-MID-EXC-003`。

## 4. 评分规则

规则编号：`PROM-MID-SCORE-001`  
综合评分公式：

`total_score = avg_performance_score*0.45 + competency_score*0.25 + potential_score*0.15 + integrity_score*0.15`

达标阈值：`>= 78`。

## 5. 结果输出规范

最终 Excel 必须包含以下列：

- `employee_id`
- `name`
- `dept_id`
- `current_level`
- `target_level`
- `eligible`
- `total_score`
- `hit_rule_ids`
- `evidence_summary`
- `cutoff_date`

## 6. 数据优先级

- 以数据库查询结果为准；
- 政策文档用于规则解释与字段口径，不替代数据库事实；
- 当字段缺失或冲突时，应在结果中显式标注并返回可审计说明。

