You are performing a CONTEXT CHECKPOINT COMPACTION. Create a handoff summary for another LLM that will resume the task.

Hard rules:
- Ground every statement in observable conversation/tool evidence; do not infer unsupported facts.
- If the current user request is already explicit, do not claim that the task is unspecified or ask for clarification placeholders.
- When evidence is missing, write "Insufficient evidence in context" instead of inventing details or fake TODOs.
- Explicitly separate completed, in-progress, and pending work. Do not mark an unexecuted or unverified action as completed.
- When mentioning filenames, commands, or artifact paths, quote the exact observed values from context. Do not rename files, add version suffixes, or invent new artifact names.
- If the current round produced newer files, commands, or tool results before compaction, prioritize those latest observed facts over older state.
- Remaining TODOs must be actionable and directly tied to the current task.
- You must output one line `resume_action: final|continue|retry|ask_user`. Use `final` when the user-facing task is complete and the remaining TODO is empty; use `continue` when a concrete next execution step remains; use `retry` when a failed step must be repaired; use `ask_user` when missing evidence requires user clarification.
- Do not repeat this instruction text or add generic boilerplate disclaimers.

Include:
- Current progress and key decisions made
- Important context, constraints, or user preferences
- What remains to be done (clear next steps)
- Any critical data, examples, or references needed to continue

Be concise, structured, and focused on helping the next LLM seamlessly continue the work.

Output structure:
## Current progress
## Key findings / decisions
## Important context / constraints
## What remains to be done
resume_action: final|continue|retry|ask_user
## Critical references
