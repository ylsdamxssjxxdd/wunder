# 智能体实时分布式系统压测与验收清单

## 1. 目标

- 建立 agent-first 压测体系，而不是只压 beeroom 页面广播。
- 验证 thread、mission、projection 三层在高并发下的行为。
- 验证慢客户端、断线恢复、owner 切换、热点隔离是否达标。

## 2. 现有基线

- [beeroom_realtime_baseline.rs](C:/Users/sjxx/Desktop/wunder/src/bin/beeroom_realtime_baseline.rs) 已经是一个可复用起点。
- 当前缺的是：
  - hot thread 基线
  - hot mission 基线
  - mixed load 基线
  - reconnect storm 与 backpressure 演练

## 3. 指标清单

### 3.1 Thread 指标

- [ ] `thread_accept_latency`
- [ ] `thread_queue_wait_latency`
- [ ] `first_token_latency`
- [ ] `thread_cancel_apply_latency`
- [ ] `thread_checkpoint_write_latency`

### 3.2 Mission 指标

- [ ] `mission_schedule_latency`
- [ ] `task_claim_latency`
- [ ] `task_result_apply_latency`
- [ ] `mission_finalize_latency`
- [ ] `mission_timeout_total`

### 3.3 Projection 指标

- [ ] `projection_snapshot_build_latency`
- [ ] `projection_delta_emit_latency`
- [ ] `projection_replay_latency`
- [ ] `projection_resync_total`
- [ ] `projection_backpressure_total`

### 3.4 路由与 Presence 指标

- [ ] `route_lease_acquire_latency`
- [ ] `route_epoch_switch_total`
- [ ] `presence_connection_total`
- [ ] `presence_watch_total`
- [ ] `presence_hidden_watch_ratio`

## 4. Benchmark 二进制清单

- [ ] 扩展 [beeroom_realtime_baseline.rs](C:/Users/sjxx/Desktop/wunder/src/bin/beeroom_realtime_baseline.rs)
- [ ] 新增 `thread_runtime_baseline.rs`
- [ ] 新增 `mission_runtime_baseline.rs`
- [ ] 新增 `projection_room_baseline.rs`
- [ ] 新增 `mixed_world_baseline.rs`

## 5. 场景压测清单

### 5.1 Hot Thread

- [ ] 同一用户高频连续消息
- [ ] 多个 thread 并发且都要求首 token
- [ ] cancel 风暴 + retry

### 5.2 Hot Mission

- [ ] 单 mission 多 task 并行
- [ ] 多 mission 同时占用同一组 agent
- [ ] merge/finalize 高频场景

### 5.3 Hot Projection

- [ ] 单 beeroom 高 fanout
- [ ] 多 conversation 同时 watch
- [ ] summary + detail + canvas 混合订阅

### 5.4 Mixed Load

- [ ] thread token 流
- [ ] mission public events
- [ ] beeroom summary fanout
- [ ] user_world social updates
- [ ] 隐藏页后台 watch

### 5.5 Failure / Chaos

- [ ] owner 节点切换
- [ ] reconnect storm
- [ ] slow client queue overflow
- [ ] snapshot 丢失后 resync
- [ ] route lease 抖动

## 6. 慢客户端演练清单

- [ ] 验证 `warn -> degrade -> resync` 是否生效
- [ ] 验证关键执行事件不丢
- [ ] 验证普通 summary/panel diff 可被合并或放弃
- [ ] 验证客户端收到 `resync_required` 后恢复时间

## 7. 回归测试清单

- [ ] Thread 单写者回归
- [ ] Mission 顺序与终态回归
- [ ] Projection snapshot + replay 回归
- [ ] PG/SQLite 双存储实现回归
- [ ] 前端隐藏页与恢复回归

## 8. SLO 验收清单

- [ ] `thread command accepted p95 < 80ms`
- [ ] `mission schedule p95 < 120ms`
- [ ] `first token visible p95 < 300ms`
- [ ] `tool result apply p95 < 150ms`
- [ ] `projection snapshot join p95 < 200ms`
- [ ] `500 event replay p95 < 500ms`
- [ ] `critical events under backpressure` 不丢

## 9. 发布前演练清单

- [ ] 单节点切 hybrid
- [ ] 双节点 route lease 演练
- [ ] 手动 kill owner 节点恢复
- [ ] 强制 slow client 降级
- [ ] 前端页面刷新/切页/隐藏恢复
- [ ] 回滚到 legacy 模式演练

## 10. 上线后观测清单

- [ ] 仪表板区分 thread / mission / projection
- [ ] 告警区分执行类问题与投影类问题
- [ ] 记录 resync 原因分布
- [ ] 记录 backpressure 触发通道分布
- [ ] 记录热点 thread/mission/projection topN
