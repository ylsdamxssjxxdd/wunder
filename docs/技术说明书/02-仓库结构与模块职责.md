# 仓库结构与模块职责

## 1. 顶层目录

```
wunder/
├── src/              Server 主 crate（API、渠道、存储、配置、观测、业务服务、orchestrator）
├── frontend/         用户侧前端（Vue 3）
├── web/              管理员侧前端（原生 HTML + JS 模块）
├── wunder-cli/       CLI 入口（含 TUI）
├── desktop/tauri/    Tauri 桌面形态
├── desktop/electron/ Electron 打包壳
├── config/           配置样例、i18n、字体、MCP 配置
├── docs/             设计文档、技术说明书、API 文档、方案
├── scripts/          仓库脚本
├── tests/            后端集成/回归测试
├── extra_mcp/        额外 MCP 运行时与工具脚本
└── packaging/        构建/分发附加资源（docker、python、windows）
```

## 2. Rust 后端分层

### 2.1 接入层

把外部请求接入内部运行时。只做鉴权、参数校验、协议适配和接线。

| 目录 | 职责 |
| --- | --- |
| `src/api/` | HTTP / WS 路由、请求编排、payload shaping |
| `src/gateway/` | Gateway 协议、节点调用、管理面消息 |
| `src/channels/` | 外部渠道适配（飞书、微信、QQ 等）、消息收发 |

### 2.2 运行时层

智能体执行的核心引擎。

| 目录 | 职责 |
| --- | --- |
| `src/orchestrator/` | 智能体执行调度主链（turn / prompt / context / tool / recovery） |
| `src/services/runtime/thread/` | 线程运行时（线程生命周期、lease、排队、dispatch） |
| `src/services/runtime/mission/` | Mission 运行时（多智能体协作任务调度） |
| `src/core/` | 配置、鉴权、审批、exec policy、i18n、schemas、state |
| `src/sandbox/` | 沙盒服务 |

### 2.3 业务服务层

领域逻辑和编排。

| 目录 | 职责 |
| --- | --- |
| `src/services/swarm/` | 协作任务、team run、聚合汇总 |
| `src/services/tools/` | 工具实现（文件、命令、浏览器、渠道消息等） |
| `src/services/memory*` | 长期记忆与自动提取、片段管理 |
| `src/services/knowledge.rs` | 知识解析与检索 |
| `src/services/vector_knowledge.rs` | 向量知识检索 |
| `src/services/workspace.rs` | 工作区与文件现实 |
| `src/services/user_world.rs` | 用户世界与消息视图 |
| `src/services/beeroom_realtime.rs` | Beeroom 实时服务 |
| `src/services/swarm/` | 蜂群协作服务 |
| `src/services/hive_pack.rs` | 蜂群资产封装 |
| `src/services/abilities/` | 智能体能力模型 |
| `src/services/inner_visible/` | 内部可见性服务 |
| `src/services/cron*` | 定时任务调度 |
| `src/services/skills.rs` | 技能管理与调用 |
| `src/services/mcp.rs` | MCP 管理与接入 |
| `src/services/bridge/` | 桥接服务与身份适配 |
| `src/services/external/` | 外部系统接入 |
| `src/services/sim_lab/` | 仿真与评估实验室 |
| `src/services/subagents.rs` | 子智能体派生与控制 |

### 2.4 基础设施层

| 目录 | 职责 |
| --- | --- |
| `src/storage/` | PostgreSQL / SQLite 统一抽象与实现 |
| `src/ops/` | 监控、性能、吞吐、基准测试 |
| `src/lsp/` | LSP 相关入口 |

## 3. Orchestrator 内部结构

`src/orchestrator/` 是 wunder 的智能体执行调度引擎，管理从请求接入到最终回复的完整链路：

```
src/orchestrator/
├── mod.rs              模块入口与公共类型
├── config.rs           运行时配置
├── constants.rs        常量定义
├── error.rs            错误类型
├── types.rs            公共类型定义
├── request.rs          请求模型
├── turn_state.rs       Turn 状态机
├── execute.rs          Turn 执行主链
├── preflight.rs        执行前预处理
├── prompt.rs           Prompt 组装与冻结
├── context.rs          上下文管理
├── context_compactor.rs 上下文压缩
├── compaction_policy.rs 压缩策略
├── memory.rs           记忆注入
├── llm.rs              模型调用
├── tool_calls.rs       工具调用解析
├── tool_exec.rs        工具执行调度
├── tool_parallel.rs    工具并行执行
├── retry_governor.rs   重试治理
├── result_normalizer.rs 结果归一化
├── event_stream.rs     事件流
├── stream_persist.rs   流式持久化
├── limiter.rs          限流器
├── runtime_snapshot.rs 运行时快照
└── thread_runtime.rs   线程运行时
```

## 4. 前端目录

### 用户侧（`frontend/`）

| 目录 | 职责 |
| --- | --- |
| `frontend/src/views/messenger/` | 聊天页视图编排与 sections |
| `frontend/src/components/messenger/` | 聊天域组件 |
| `frontend/src/components/chat/` | 聊天通用组件 |
| `frontend/src/realtime/` | Beeroom 实时消费者 |
| `frontend/src/stores/` | 状态存储 |
| `frontend/src/api/` | 请求层 |

### 管理员侧（`web/`）

| 目录 | 职责 |
| --- | --- |
| `web/modules/` | 管理域模块（monitor、tools、llm、channels 等） |
| `web/styles/` | 样式 |
| `web/docs/` | 静态文档资源 |

## 5. 常见改动落点

| 要做的事 | 放在哪里 | 不要放 |
| --- | --- | --- |
| 新增 API | `src/api/*` + `src/services/*` | 在路由里堆业务逻辑 |
| 新增工具实现 | `src/services/tools/*.rs` | 往旧 `src/services/tools.rs` 堆 |
| 新增运行时语义 | `src/orchestrator/*` | 在 API 层新造执行链路 |
| 新增线程调度 | `src/services/runtime/thread/*` | 跳过 ThreadRuntime 直接操作 |
| 新增前端页面能力 | `frontend/src/views/messenger/` + `frontend/src/components/messenger/` | 往 `MessengerView.vue` 堆 |
| 新增管理页能力 | `web/modules/*` | 在 `web/app.js` 写耦合逻辑 |

## 6. 改代码前的判断顺序

1. 这是**接入层**问题、**运行时**问题、**投影**问题还是**业务服务**问题？
2. 这段逻辑是**核心执行语义**还是**产品适配**？
3. 操作的是 **durable state**、**实时状态** 还是**进程内临时状态**？
4. 改动是否需要同步更新设计文档、API 文档或本说明书？

## 7. 关联文档

- `docs/设计文档/02-智能体核心设计.md`
- `docs/设计文档/01-系统总体设计.md`
- `docs/设计文档/03-实时投影系统设计.md`
