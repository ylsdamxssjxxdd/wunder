# 智能体内见系统实施清单

> 设计基线见：`docs/智能体内见系统落地方案.md`

## A. 目录与协议

- [x] 内见根目录统一为用户私有目录（`container_id=0`）
- [x] 内见目录收敛为 `agents/skills/knowledge/global`
- [x] 删除 `.wunder` 目录依赖（结构与提示词）
- [x] 内见协议提示词改为“HOME + 相对目录”，避免重复绝对路径
- [x] 内见协议提示词增加“当前智能体 ID / 当前工蜂卡路径”

## B. agents 扁平化

- [x] 工蜂卡路径改为 `agents/<agent_id>.worker-card.json`
- [x] 停止写入 `agents/<agent_id>/system_prompt.md`
- [x] 停止写入 `agents/<agent_id>/skills/`
- [x] 停止写入 `agents/**/README.md`
- [x] 兼容读取旧路径 `agents/<agent_id>/worker-card.json`
- [x] 同步写回后清理旧目录 `agents/<agent_id>/`

## C. 同步链路

- [x] 请求前内见同步仍生效（运行态）
- [x] 设置页读取前触发同步（读态）
- [x] 用户/智能体直接改 `.worker-card.json` 后，设置页可见字段可同步
- [x] 线程 system prompt 冻结语义保持不变

## D. 异常与稳定性

- [x] 解析失败时保留现有生效配置，不因单文件异常中断主流程
- [x] 异常写入服务端告警日志（无 `.wunder` 诊断文件）
- [x] 保持最小改动、并发安全与低额外资源开销

## E. 文档同步

- [x] 更新 `docs/智能体内见系统落地方案.md`
- [ ] 更新 `docs/智能体设计.md`（内见系统章节）
- [ ] 更新 `docs/API文档.md`（如新增/调整接口行为时）
